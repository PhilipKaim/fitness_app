import React from 'react';

const Footer = (props) => {
        return (
            <div style={style.container}>
                footer
            </div>
        );
}

const style = {
    container: {
        width: '100%',
        height: '300px',
        backgroundColor: 'cornflowerblue'
    }
}

export default Footer;