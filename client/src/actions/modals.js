export const signinModal = (visible, modal) => ({
    type: 'SIGNIN_MODAL',
    visible,
    modal
});

export const editFoodModal = (visible, modal) => ({
    type: 'EDIT_FOOD_MODAL',
    visible,
    modal
});

export const addFoodModal = (visible, modal) => ({
    type: 'ADD_FOOD_MODAL',
    visible,
    modal
});